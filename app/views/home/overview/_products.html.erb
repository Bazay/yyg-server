<p>The Product table holds the base product, as well as a type of product. A product_type allows categorisation of products (I've only examined the Game Maker product type).</p>
<hr>

<% products.group_by(&:product_type).each do |product_type, products_group| %>
  <h4><%= product_type.humanize.titleize %></h4>
  <table class="table table-hover table-condensed">
    <thead>
      <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Product Type</th>
        <th>Base Price</th>
        <th>Deleted</th>
        <th>Deleted At</th>
        <th>Created At</th>
        <th>Updated At</th>
      </tr>
    </thead>
    <tbody>
      <% products_group.each do |product| %>
        <tr>
          <td><%= product.id %></td>
          <td><%= product.name %></td>
          <td><%= product.product_type %></td>
          <td><%= product.base_price %></td>
          <td><%= product.deleted %></td>
          <td><%= product.deleted_at %></td>
          <td><%= product.created_at.strftime("%H:%M %d/%m/%Y") %></td>
          <td><%= product.updated_at.strftime("%H:%M %d/%m/%Y") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>