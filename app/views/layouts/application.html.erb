<!DOCTYPE html>
<html>
<head>
  <meta name="description" content="YoYo Games Web Development Test">
  <meta name="author" content="Baron Bloomer">
  <title><%= content_for?(:title) ? "YoYo Games | #{yield(:title).try(:humanize).try(:titleize)}" : "YoYo Games Server" %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <!-- Header -->
  <%= render 'layouts/header' %>

  <div id="main" role="main" class="<%= controller_name %>_<%= action_name %>">

    <!-- Flash Messages -->
    <%= render 'layouts/messages' %>

    <div class="container">
      <%= yield %>
    </div><!-- /.container -->
  </div><!-- /#main -->

</body>
</html>
